[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "mazegen"
version = "1.0.0"
description = "A reusable maze generator library"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["mazegen*"]
